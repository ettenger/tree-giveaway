<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @giveaway.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @giveaway.description %>
</p>

<p>
  <strong>Location:</strong>
  <%= @giveaway.location %>
</p>

<p>
  <strong>Trees Available:</strong>
  <br>
  <% @giveaway.trees.each do |tree| %>
  <% input_id = "request_trees_" + tree.id.to_s %>
  <%= radio_button_tag("request[trees]", tree.id) %>
  <%= label_tag(input_id, tree.name) %>
  <div> <%= tree.stock %> Remaining </div>
  <p><%= tree.description %></p>
  <br>
  <% end %>

  <!-- <%= collection_radio_buttons(:request, :trees, @giveaway.trees, :id, :name) %> -->
</p>



<%= link_to 'Edit', edit_giveaway_path(@giveaway) %> |
<%= link_to 'Back', giveaways_path %>
